# 项目规划：手语桥（SignBridge）端侧手语识别终端

> 主要目标板：ESP32-P4-Function-EV-Board
> 只有在板子上实际跑通的才算完成，代码写了没验证的标"未验证"。

---

## 1. 功能清单与开发计划

### 功能 1：手语手势采集与实时识别（MIPI摄像头 + 双核RISC-V算力）

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| SC2336 I2C 初始化代码 | 代码已写，未验证 | 从未在板子上跑过 |
| MIPI CSI 驱动框架 | 代码已写，未验证 | 从未在板子上跑过 |
| 摄像头模块状态机 | 代码已写，未验证 | 从未在板子上跑过 |
| 手部关键点模型（MediaPipe） | 代码已写，未验证 | 从未在板子上跑过 |
| INT8 MLP 时序分类器 | 代码已写，未验证 | 从未在板子上跑过 |
| **摄像头→模型→识别结果 全链路** | **未完成** | 从未跑通 |

### 功能 2：翻译结果语音播报（I2S音频输出 + 扬声器）

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| ES8311 codec 驱动 | 代码已写，未验证 | 从未在板子上出过声 |
| I2S0 音频输出 | 代码已写，未验证 | 从未在板子上出过声 |
| nxplayer 播放接口 | 代码已写，未验证 | 从未在板子上出过声 |
| **识别结果→语音播报** | **未完成** | audio_beep.c 函数体为空 |

### 功能 3：语音输入采集与识别（I2S数字麦克风）

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| 麦克风采集驱动 | 代码已写，未验证 | 从未在板子上验证 |
| VAD 接口 | 代码已写，未验证 | 从未在板子上验证 |
| **唤醒词模型** | **未完成** | 未训练 |
| **语音→文字 ASR** | **未完成** | 未实现 |

### 功能 4：手语动画驱动与展示（7寸触摸屏 + PPA像素加速器）

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| EK79007AD MIPI DSI 显示 | **已在板子上跑通** | 1024×600 正常显示 |
| GT911 触摸轮询 | **已在板子上跑通** | 触摸响应正常 |
| LVGL 界面框架 | **已在板子上跑通** | 1024×600 布局正常 |
| 开机画面 | **已在板子上跑通** | logo + 进度条 |
| 主界面 UI | **已在板子上跑通** | 摄像头预览区+识别结果卡片+按钮 |
| **动画与识别结果联动** | **未完成** | 识别结果未接入 |

### 功能 5：云端语义纠错与补全（WiFi6）

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| WiFi6 驱动 | 代码已写，未验证 | 从未在板子上验证 |
| **纠错应用层** | **未完成** | 未实现 |
| **云端 API** | **未完成** | 未实现 |

### 功能 6：便携续航与低功耗待机

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| 背光管理 | 代码已写，未验证 | 从未在板子上验证 |
| **触摸中断唤醒** | **未完成** | INT/RESET 未接主控 |
| **深睡模式** | **未完成** | 依赖触摸中断 |

---

## 2. 实际完成情况

**已在板子上跑通**：
- 7寸 MIPI DSI 显示（EK79007AD 1024×600）
- GT911 触摸轮询
- LVGL 界面 + 开机画面 + 主界面 UI

**代码已写但从未在板子上验证**：
- 摄像头 SC2336 + MIPI CSI
- 音频 ES8311 + I2S
- 麦克风采集 + VAD
- WiFi6
- 推理模型（MediaPipe + MLP）
- 背光管理

**完全未实现**：
- 推理管线集成（摄像头→模型→识别结果）
- 语音播报（识别结果→扬声器）
- 语音唤醒词
- 云端纠错
- 深睡模式

---

## 3. 代码归属

| 内容 | 归属 | 说明 |
| --- | --- | --- |
| ESP32-P4 芯片支持 | nuttx 公共仓 | PR 到 open-vela/nuttx |
| Function-EV-Board 板级 | nuttx 公共仓 | PR 到 open-vela/nuttx |
| gt9xx 触摸驱动 | nuttx 公共仓 | PR 到 open-vela/nuttx |
| lvgldemo 手语界面 | 专属仓 | app/signbridge/ |

---

## 4. 专属仓上传清单

待用户审核确认后执行：

| 目标位置 | 来源 | 文件 |
| --- | --- | --- |
| `app/signbridge/` | `apps/examples/lvgldemo/` | lvgldemo.c, ui_main.c/h, boot_screen.c/h, camera_module.c/h, audio_beep.c, logo_data.c/h, zh_font_16.c, zh_font_40.c |

---

## 5. 待用户确认

1. 上述功能清单和状态评估是否准确？
2. nuttx 内核改动（ESP32-P4 + gt9xx）是否需要上传到专属仓？还是只走公共仓 PR？
3. 专属仓上传内容是否需要调整？
4. 先重置 fork 仓库再上传，还是在现有 fork 上操作？
